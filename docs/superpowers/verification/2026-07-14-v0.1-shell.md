# ClipNote V0.1 Shell Verification

- Frontend unit tests: PASS — `pnpm test`: 7 files, 18 tests.
- Frontend lint: PASS — `pnpm lint`.
- Frontend production build: PASS — `pnpm build`.
- Browser E2E and visual regression: PASS — `pnpm test:e2e`: 2 tests.
- Rust formatting, tests, and cargo check: PASS — `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`, `cargo test --manifest-path src-tauri/Cargo.toml`, and `cargo check --manifest-path src-tauri/Cargo.toml`; 2 Rust tests.
- Native edge docking: PASS — a fresh `pnpm tauri dev` launch started directly at 112×322 at (1796, 379); Alt+Space expanded to 648×1000 at (1260, 40); the collapse control returned to 112×322 at (1796, 379).
- Escape collapse: PASS in the earlier native run. The final automation used the visible collapse control because synthetic Escape delivery was not reliable after the foreground application changed.
- Alt+Space toggle: PASS — observed native global shortcut activation changed collapsed → expanded geometry.
- Shell-mode reload recovery: PASS — after expanding the native window, F5 reloaded the WebView and the expanded workspace remained rendered at 648×1000 instead of reverting to the edge tab.
- Tray open/pause/quit: NOT VERIFIED — no safe automation was available to identify and operate the system-tray icon/menu without interacting with unrelated desktop content. Source inspection is not counted as a native observation.
- Reduced-motion behavior: NOT VERIFIED — Windows Animation effects was not changed.
- Tested display scale(s): current Windows session only. The initial configuration produced a 136-pixel logical-width window before the startup docking fix, indicating 125% scaling; the final native geometry used the expected physical 112×322 and 648×1000 bounds. No additional scale was tested.
- Product screenshots: TEMPORARY EVIDENCE ONLY — window-only captures were reviewed from the temporary directory; no unrelated desktop content or `docs/assets/v0.1/` product files were stored.
- Known limitations: Fixture content only; live clipboard, persistence, note editing, privacy controls, and AI remain outside V0.1 shell scope. Tray, reduced-motion, and multiple-display-scale checks remain manually unverified.

## Shell-mode synchronization regression

- App-level native event reaction, late-listener cleanup, and rejected-listener handling: PASS — `src/app/App.test.tsx`.
- Listener bootstrap ordering: PASS — `desktopBridge` registers `shell-mode-changed` before reading `get_main_window_mode`, ignores a stale read if an event arrives during setup, and restores the native mode after WebView reload.
- Initial mode read failure: PASS — a failed `get_main_window_mode` read keeps the successfully registered listener alive so future native events can recover synchronization.
- Native expand/collapse commands emit `shell-mode-changed`: exercised through the running application; Rust geometry tests also pass.
- Duplicate native mode publication: PASS — same-mode `setMode`, `expand`, and `collapse` calls do not republish Zustand state.
- Fresh-launch docking: PASS — Tauri setup applies collapsed geometry before the renderer completes its native-mode handshake.
